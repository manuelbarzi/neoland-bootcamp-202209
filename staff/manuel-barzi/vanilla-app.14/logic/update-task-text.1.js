function updateTaskText(userEmail, taskId, text) {
    // TODO find task in db by id, validate it belongs to user (email), and update its text

    console.log('update task', userEmail, taskId, text)

    return null
}