function indexof(array, element, index) {

    if (n > array.length - 1)
        aux = 0

    if (n >0 && n < array.length -1)
        aux = index
    
    // if  (start )
        
        for (var i = aux; i < array.length; i++) {
        if (array[i] === element)
            result = i

    }

    return result

}