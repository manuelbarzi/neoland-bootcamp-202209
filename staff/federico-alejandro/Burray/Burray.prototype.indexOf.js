Burray.prototype.indexOf = function(searchElement, fromIndex = 0) {
    let result = ''
    
    for (let i = fromIndex; i < this.length; i++)
        if(this[i] === searchElement)

        return result
}