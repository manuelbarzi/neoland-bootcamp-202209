var tasksTodoColumn =[]

function createTask(user, text) {
  //user, text.preventDefault();
  var taskCard ={
    id,
    user,
    text,
  }
  tasksTodoColumn.push(taskCard);
  return null;
}

// NO SE COMO FUNCIONA
  /*for (var i = 0; i < task.length; i++) {
    var task = tasks[i];
  }
  var tasks = {
    id: taskCard++,
    user: email,
    text: buttonTasksInput.value,
    status: "",
  };*/

  

  

// TODO create a task for the user (email) with provided text in db

/*var taskCard = document.createElement("article");

  taskCard.innerText = text;

  //buttonTasks.addEventListener('click', createTaskCard)//agrega  tarea

  taskCard.className ="container container--border-i container--padding-s container--full-width";

  return taskCard;*/
