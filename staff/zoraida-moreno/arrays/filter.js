function myFilter (array, callback) {
    var newArray = []

for(var i = 0; i < array.length; i++) {
    var firltrado = numero.filter(numero => esMenorA10(numero))
}
}


var filtrado = numeros.filter(numero => esMenorA10(numero))
// var filtrado2 = numeros.filter(numero => numero > 10)