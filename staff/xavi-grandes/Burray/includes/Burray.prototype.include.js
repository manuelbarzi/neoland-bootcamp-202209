Burray.prototype.includes = function(b, callback){
    
    for (var i = 0; i< this.length; i++)
        element = this.length[i]
        callback (element)

        if (callback === true){
            return true
        }
    
        return false
}